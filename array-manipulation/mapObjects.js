// Object.entries(obj) converts our object into an array of [key, value] pairs that we can then iterate over

var obj = { mac: 'priest', dennis: 'calculating', charlie: 'birdlaw', dee: 'bird', frank: 'warthog' };

console.log(Object.entries(obj))